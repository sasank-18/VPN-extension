# 🌐 Proxy Switcher Chrome Extension  

A Chrome Extension that fetches free proxies from an API, shows their IPs with location, and allows users to connect to them.  

---

Commands to run the extension on your local machine

### 1️⃣ Clone the repository  
```bash
git clone <your-repo-url>
cd <your-repo-folder>
-> npm install
-> npm run build
```

If wants to run locally then run,
```bash
npm run dev
```


If wants to run as a Extension then, take the dist folder and upload it in the browser extension, step are here,
```bash
go to -> chrome://extensions/
Enable Developer Mode (top right corner).

Click Load unpacked.

Select the dist/ folder generated after build.

✅ Your extension should now appear in the Chrome toolbar

```
